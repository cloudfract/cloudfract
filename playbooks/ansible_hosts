vagrant01   ansible_ssh_host=10.0.10.75    ansible_ssh_user=vagrant   ansible_ssh_private_key_file=../vagrant/Vagrant.pem

[vagrant]
vagrant01

[api]
vagrant01

[generator]
vagrant01

[registry]
vagrant01

[store]
vagrant01
